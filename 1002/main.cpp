#include <cstdlib>
#include <cstdio>
#include <cstring>
const int maxn = 5005;
char number[maxn];
char substr[57][maxn];
int main()
{
    scanf("%s",number);
    int n;
    scanf("%d",&n);
    for (int i=0;i<n;i++)
        scanf("%s",substr[i]);
    
    return 0;
}